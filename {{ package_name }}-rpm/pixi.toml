[workspace]
authors = ["Jakub Wlodek <jwlodek@bnl.gov>"]
channels = ["conda-forge"]
name = "{{ package_name }}-rpm"
platforms = ["linux-64"]
version = "0.1.0"

[tasks]

[dependencies]
copier = ">=9.14.0,<10"
python = ">=3.13,<3.14"
